const defaultState = [];

const reducer = (state = defaultState, { type, payload }) => {
  switch (type) {
    case "createGame": {
      return 
    }
    default:
      return state;
  }
};

export default reducer